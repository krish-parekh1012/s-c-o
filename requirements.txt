streamlit>=1.22
numpy>=1.23
pandas>=1.3
pillow>=9.0
requests>=2.28
# google-generative-ai is optional and can break builds on Streamlit Cloud.
# If you want LLM support, we'll install it at runtime conditionally.
